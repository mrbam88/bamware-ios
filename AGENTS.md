# AGENTS.md — bamware-ios

> Read [`../bamware-ai/AGENTS.md`](https://github.com/mrbam88/bamware-ai/blob/main/AGENTS.md)
> first: the Bamware system map (repos, contracts, gates, security).
> Missing siblings? Run bamware-ai `scripts/bootstrap.sh`. This file is
> ios-package specific.

## What it is

Reusable Swift package products — Core, UI, Messaging. Tenant-aware native
foundation consumed by native apps (e.g. `bamware-brewdesk`). Workflow,
workspace substitution, and package-test conventions: bamware-ai skill
`native-ios-workspace`.
